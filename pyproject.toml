[project]
name = "flow-diffuse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "diffusers>=0.39.0",
    "ipykernel>=7.3.0",
    "numpy>=2.2.6",
    "s3fs>=2026.6.0",
    "torch>=2.13.0",
    "zarr>=2.18.3",
]
